# Data Directory

This directory contains application data files that are synced via OneDrive.

## Files:
- `calendar-notes.json` - Calendar notes storage (todo, timesheet, diary, meetings)

**Note:** This directory is excluded from git (see `.gitignore`) but synced via OneDrive for cloud persistence.
